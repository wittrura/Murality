<%- include ../partials/header.ejs %>

    <h3>Mural Tours Around Austin</h3>
    <h4>Create your own Tours or See What Others have Made</h4>
    <% let curTourId = 0; %>
    <% let tourCount = 1; %>
    <% let firstList = true; %>
      <% toursMurals.forEach(tour => { %>


        <% if (tour.tour_id !== curTourId) { %>
          <% if (firstList) { %>
            <% firstList = false; %>
          <% } else { %>
          </ol>
          <% } %>
          <h4><%= tourCount++ %></h3>
          <ol>
        <% } %>
            <li><%= tour.muralName + ', ' + tour.artistName %></li>
      <% }) %>
    </ol>

<%- include ../partials/footer.ejs %>
